{
  "name": "bakend",
  "version": "1.0.1",
  "description": "PocketBase + Functions + Jobs — the simplest backend platform for MVPs",
  "type": "module",
  "private": true,
  "bin": {
    "bak": "./src/index.ts"
  },
  "exports": {
    "./functions": "./src/core/functions/triggers.ts"
  },
  "workspaces": [
    "sdk/javascript"
  ],
  "scripts": {
    "start": "bun run src/index.ts start",
    "dev": "bun run src/index.ts dev",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test",
    "test:sdk": "bun test tests/sdk",
    "sdk:build": "cd sdk/javascript && bun run build",
    "sdk:check": "cd sdk/javascript && bun run build",
    "test:dart": "cd sdk/dart && dart pub get && dart test",
    "lint": "echo 'Lint not configured yet'",
    "dashboard:dev": "cd dashboard && bun run dev",
    "dashboard:build": "cd dashboard && bun run build",
    "dashboard:check": "cd dashboard && bun run check",
    "build": "bun run scripts/build.ts",
    "build:linux-x64": "bun run scripts/build.ts --target bun-linux-x64",
    "build:linux-arm64": "bun run scripts/build.ts --target bun-linux-arm64",
    "build:darwin-arm64": "bun run scripts/build.ts --target bun-darwin-arm64",
    "build:darwin-x64": "bun run scripts/build.ts --target bun-darwin-x64"
  },
  "dependencies": {
    "@bakend/client": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
